<div class="container">
  <div class="my-5">
  <h1>Add your Toothbrush</h1>
    <%= simple_form_for(@toothbrush) do |f| %>
      <%= f.input :title %>
      <%= f.input :category, collection: ['Electric', 'Standard'], include_blank: 'Select Category'%>
      <%= f.input :price, input_html: { step: 'any', class: 'form-control', placeholder: '€' } %>
      <%= f.input :photo, as: :file %>
      <%= f.submit "Create Toothbrush", class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
